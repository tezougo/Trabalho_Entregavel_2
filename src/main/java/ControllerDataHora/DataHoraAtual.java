package ControllerDataHora;// para o springer encontrar essa classe

import org.springframework.stereotype.Controller;
//import org.springframework.web.bind.annotation.RequestMapping;
//import org.springframework.web.bind.annotation.ResponseBody;

@Controller
public class DataHoraAtual {
    //@RequestMapping("/")  //para dizer qual a url, quando o spring deve chamar essa página
    //@ResponseBody  // para não navegar para uma página, se não colocar ele ira procurar uma página
}